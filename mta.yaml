ID: myepm
_schema-version: '2.0'
version: 0.0.1

modules:
 - name: epmdb
   type: hdb
   path: epmdb
   requires:
    - name: hdi_epmdb

resources:
 - name: hdi_epmdb
   parameters:
      config:
         schema: epm
   properties:
      hdi-container-name: ${service-name}
   type: com.sap.xs.hdi-container
